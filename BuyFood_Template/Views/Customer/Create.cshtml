@model BuyFood_Template.ViewModel.CCustomerCreateViewModel

@{
    ViewData["Title"] = "Create";
}

<!DOCTYPE html>


<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Sign Up Form by Colorlib</title>

    <!-- Font Icon -->
    <link rel="stylesheet" href="~/fonts/create_material-icon/css/material-design-iconic-font.min.css">

    <!-- Main css -->
    <link rel="stylesheet" href="~/css/create_style.css">
    <script src="https://code.jquery.com/jquery-1.12.4.min.js"></script>

    <!--裁切圖片-->
    <link href='//maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css' rel='stylesheet'></link>
    <link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css" rel="stylesheet"></link>
    <script src="//ajax.googleapis.com/ajax/libs/jquery/2.0.0/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/croppie/2.6.5/croppie.min.js"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/croppie/2.6.5/croppie.css" rel="stylesheet"></link>



    <style>

        body {
            padding: 0px;
            background-color: white;
        }

        .header_top {
            background-color: #F5F5F5;
        }

        .col-lg-6 div {
            background-color: #F5F5F5;
        }

        .col-md-6 {
            background-color: #F5F5F5;
        }

        input[type=radio] {
            margin: 1px;
        }
    </style>

</head>



<div class="signup-content">
    @*<div class="signup-img">
            <img src="images/signup-img.jpg" alt="">
        </div>*@
    <div class="signup-form" style="margin-left:210px; width:100%; box-shadow:4px 4px 8px 8px #cccccc;">
        <form asp-action="Create" enctype="multipart/form-data" method="post" class="register-form" id="register-form">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <h2>註冊</h2>
            <div class="form-row">
                <div class="form-group">
                    <label asp-for="CName" for="name">*姓名 :</label>
                    <input asp-for="CName" id="name" required />
                    <span asp-validation-for="CName" class="text-danger" id="spname"></span>
                </div>
            </div>
            <div class="form-row">
                <div class="form-group">
                    <label asp-for="CPassword" for="name">*密碼 :</label>
                    <input asp-for="CPassword" type="password" id="password" required />
                    <span asp-validation-for="CPassword" class="text-danger" id="sppassword"></span>
                    <span>((1.不可空白 , 2.至少6個字且必須包含英文字母+數字)</span>
                    <label style="color:red" id="Wrongpassword"></label>
                </div>
            </div>
            <div class="form-row">
                <div class="form-group">
                    <label for="name">*確認密碼 :</label>
                    <input type="password" name="checkPassword" id="checkPassword" required />
                    <label style="color:red" id="WrongCheckpassword"></label>
                </div>
            </div>
            <div class="form-radio">
                <label asp-for="CGender" for="gender" style="padding-right:20px;">*性別 :</label>
                <div class="form-check form-check-inline">
                    @*暫時替代*@
                    <input asp-for="CGender" id="inlineRadio1">
                </div>
                @*<div class="form-check form-check-inline">
                        <input asp-for="CGender" type="radio" id="inlineRadio1" value="male">
                        <label class="form-check-label" for="inlineRadio1">男</label>
                    </div>*@
                @*<div class="form-check form-check-inline">
                        <input  asp-for="CGender" type="radio" id="inlineRadio2" value="female">
                        <label class="form-check-label" for="inlineRadio2">女</label>
                    </div>*@
            </div>
            <div class="form-row">
                <div class="form-group">
                    <label asp-for="CAge" for="age">*生日 :</label>
                    <input asp-for="CAge" type="date" id="age" required />
                    <span asp-validation-for="CAge" class="text-danger" id="spage"></span>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="CAddress" for="address">*地址 :</label>
                <input asp-for="CAddress" id="address" required />
                <span asp-validation-for="CAddress" class="text-danger" id="spaddress"></span>
            </div>
            <div class="form-row">
                <div class="form-group">
                    <label asp-for="CPhone" for="name">*手機 :</label>
                    <input asp-for="CPhone" id="phone" required />
                    <span asp-validation-for="CPhone" class="text-danger" id="spphone"></span>
                </div>
            </div>
            <div class="form-group">
                <label asp-for="CEmail" for="email" style="position:relative">*Email :</label>
                <input asp-for="CEmail" id="email" required />
                <span asp-validation-for="CEmail" class="text-danger" id="spemail"></span>
                <span id="Wrongemail"></span>
            </div>



            <label asp-for="CPicture" style="position:relative">上傳頭像 :</label>
                <input type="file" name="img" id="image" style="padding:0px; border:none" />
                <span asp-validation-for="CPicture" class="text-danger" id="spimg"></span>

            @*裁切圖片*@
            @*<label class="btn btn-info"><input id="upload_img" style="display:none;" type="file" accept="image/*"><i class="fa fa-photo"></i> 上傳圖片</label>

            <div id="oldImg" style="display:none;"></div>

            <button id="crop_img" class="btn btn-info"><i class="fa fa-scissors"></i> 裁剪圖片</button>

            <div id="newImgInfo"></div>
            <div id="newImg"></div>*@



            <div class="form-row">
                <div class="form-group">
                    <label for="code">邀請碼 :</label>
                    <input type="text" name="code" id="code" />
                </div>
            </div>

            <div class="form-submit">
                <input type="submit" value="Reset All" class="submit" name="reset" id="reset" />
                <input type="submit" value="確認註冊" class="submit" name="submit" id="submit" onclick="return confirm('確定要註冊嗎?')" />
            </div>

            <div class="form-submit">
                <input type="button" value="Demo" class="btn btn-primary"
                       onclick='(function () {
    document.getElementById("name").value = "jotaro";
    document.getElementById("email").value = "jotaro@Gmail.com";
    document.getElementById("password").value = "123";
    document.getElementById("checkPassword").value = "123";
    document.getElementById("inlineRadio1").value = "男";
    document.getElementById("address").value = "Japan";
    document.getElementById("phone").value = "0912345678";
                            })()' />
            </div>

        </form>
    </div>
</div>


<!-- JS -->
<script src="~/vendor/jquery/create_jquery.min.js"></script>
<script src="~/js/create_main.js"></script>
<!-- This templates was made by Colorlib (https://colorlib.com) -->

<script>
    $(function closeChat() {
        $("#sidebar_secondary").css("display", "none")
        $("#addClass").css("display", "none")
    })
</script>

@{
    string o = "@";
}
<script>
    document.getElementById("password").onblur = checkPwd
    document.getElementById("checkPassword").onblur = checkCheckPassword
    document.getElementById("email").onblur = checkEmail
    document.getElementById("submit").onblur = checkAll
    let flagCheckpwd = false, flagCheckCheckpwd = false, flagCheckEmail = false;


    function checkPwd() {
        let pwdObj = document.getElementById("password");
        let pwdObjVal = pwdObj.value;
        let spObj = document.getElementById("Wrongpassword");
        let pwdObjValLen = pwdObjVal.length;
        let flag1 = false, flag2 = false;

        console.log(pwdObjVal);

        if (pwdObjValLen <= 6) {
            spObj.innerHTML = "密碼必須大於6個字"
        }
        else if (pwdObjValLen > 6) {
            for (let i = 0; i < pwdObjValLen; i++) {
                let ch = pwdObjVal.charAt(i).toUpperCase();
                if (ch >= "A" && ch <= "Z")
                    flag1 = true;
                else if (ch >= "0" && ch <= "9")
                    flag2 = true;

                if (flag1 == true && flag2 == true)
                    break;
            }

            if (flag1 == true && flag2 == true) {
                spObj.innerHTML = "密碼格式正確";
                flagCheckpwd = true;
            }
            else
                spObj.innerHTML = "密碼格式錯誤"
        }
    }

    function checkCheckPassword() {
        let pwdObjVal = document.getElementById("password").value;
        let checkPwdObjVal = document.getElementById("checkPassword").value;
        let spObj = document.getElementById("WrongCheckpassword");

        if (pwdObjVal == "") {
            spObj.innerHTML = "";
        }
        else if (pwdObjVal != checkPwdObjVal) {
            spObj.innerHTML = "密碼確認錯誤"
        }
        else if (pwdObjVal == checkPwdObjVal) {
            spObj.innerHTML = "密碼確認相同";
            flagCheckCheckpwd = true;
        }
    }

    @*function checkEmail() {
        $.ajax({
            url: "@Url.Action("checkEmail", "Customer")",
            data: { "email": $("#email") },
            type: "POST",
            success: function (data) {
                let ObjEmail = document.getElementById("email");
                let ObjEmailVal = ObjEmail.value;
                console.log(data);

                if (data == "此信箱可使用") {

                    if (ObjEmailVal.length >= 5 && ObjEmailVal.match(/^[0-9a-zA-Z]([-._]*[0-9a-zA-Z])*"@o"[0-9a-zA-Z]([-._]*[0-9a-zA-Z])*\.+[a-zA-Z]+$/)) {
                        document.getElementById("Wrongemail").innerHTML = "信箱格式正確"
                    }
                    else {
                        document.getElementById("Wrongemail").innerHTML = "信箱格式錯誤"
                    }

                }
                else {
                    document.getElementById("Wrongemail").innerHTML = data;
                }

                }
            })*@
          //}
</script>


<script>
    (function ($) {
        var width_crop = 300, // 圖片裁切寬度 px 值
            height_crop = 200, // 圖片裁切高度 px 值
            type_crop = "circle", // 裁切形狀: square 為方形, circle 為圓形
            width_preview = 350, // 預覽區塊寬度 px 值
            height_preview = 350, // 預覽區塊高度 px 值
            compress_ratio = 0.85, // 圖片壓縮比例 0~1
            type_img = "jpg", // 圖檔格式 jpeg png webp
            oldImg = new Image(),
            myCrop, file, oldImgDataUrl;

        // 裁切初始參數設定
        myCrop = $("#oldImg").croppie({
            viewport: { // 裁切區塊
                width: width_crop,
                height: height_crop,
                type: type_crop
            },
            boundary: { // 預覽區塊
                width: width_preview,
                height: height_preview
            }
        });

        function readFile(input) {
            if (input.files && input.files[0]) {
                file = input.files[0];
            } else {
                alert("瀏覽器不支援此功能！建議使用最新版本 Chrome");
                return;
            }

            if (file.type.indexOf("image") == 0) {
                var reader = new FileReader();

                reader.onload = function (e) {
                    oldImgDataUrl = e.target.result;
                    oldImg.src = oldImgDataUrl; // 載入 oldImg 取得圖片資訊
                    myCrop.croppie("bind", {
                        url: oldImgDataUrl
                    });
                };

                reader.readAsDataURL(file);
            } else {
                alert("您上傳的不是圖檔！");
            }
        }

        function displayCropImg(src) {
            var html = "<img src='" + src + "' />";
            $("#newImg").html(html);
        }

        $("#upload_img").on("change", function () {
            $("#oldImg").show();
            readFile(this);
        });

        oldImg.onload = function () {
            var width = this.width,
                height = this.height,
                fileSize = Math.round(file.size / 1000),
                html = "";

            html += "<p>原始圖片尺寸 " + width + "x" + height + "</p>";
            html += "<p>檔案大小約 " + fileSize + "k</p>";
            $("#oldImg").before(html);
        };

        function displayNewImgInfo(src) {
            var html = "",
                filesize = src.length * 0.75;

            html += "<p>裁切圖片尺寸 " + width_crop + "x" + height_crop + "</p>";
            html += "<p>檔案大小約 " + Math.round(filesize / 1000) + "k</p>";
            $("#newImgInfo").html(html);
        }

        $("#crop_img").on("click", function () {
            myCrop.croppie("result", {
                type: "canvas",
                format: type_img,
                quality: compress_ratio
            }).then(function (src) {
                displayCropImg(src);
                displayNewImgInfo(src);
            });
        });
    })(jQuery);
</script>